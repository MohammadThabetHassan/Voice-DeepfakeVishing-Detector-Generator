# Backend dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydub>=0.25.1
scipy>=1.11.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Optional: install for real voice cloning
# TTS>=0.22.0     # Coqui TTS (large, requires PyTorch)

# Optional: gTTS fallback (lightweight, internet required)
gtts>=2.5.0

# Optional: ffmpeg must be installed via system package manager for format conversion:
#   Ubuntu/Debian: sudo apt-get install -y ffmpeg
#   macOS:         brew install ffmpeg
#   Windows:       choco install ffmpeg
