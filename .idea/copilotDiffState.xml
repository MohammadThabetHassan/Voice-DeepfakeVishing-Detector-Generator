<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/index.html">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/index.html" />
              <option name="originalContent" value="&lt;!DOCTYPE html&gt;&#10;&lt;html lang=&quot;en&quot;&gt;&#10;&lt;head&gt;&#10;    &lt;meta charset=&quot;UTF-8&quot; /&gt;&#10;    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&gt;&#10;    &lt;title&gt;Deepfake Vishing Demo&lt;/title&gt;&#10;    &lt;style&gt;&#10;        body {&#10;            font-family: Arial, sans-serif;&#10;            background-color: #f5f7fa;&#10;            margin: 0;&#10;            padding: 0;&#10;        }&#10;        header {&#10;            background-color: #2b2d42;&#10;            color: white;&#10;            padding: 1rem;&#10;            text-align: center;&#10;        }&#10;        main {&#10;            max-width: 800px;&#10;            margin: 2rem auto;&#10;            background: white;&#10;            border-radius: 8px;&#10;            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);&#10;            padding: 2rem;&#10;        }&#10;        h2 {&#10;            color: #2b2d42;&#10;            margin-top: 0;&#10;        }&#10;        form {&#10;            margin-bottom: 2rem;&#10;        }&#10;        label {&#10;            display: block;&#10;            margin-bottom: 0.5rem;&#10;            font-weight: bold;&#10;        }&#10;        input[type=&quot;file&quot;],&#10;        input[type=&quot;text&quot;] {&#10;            width: 100%;&#10;            padding: 0.5rem;&#10;            margin-bottom: 1rem;&#10;            border: 1px solid #ccc;&#10;            border-radius: 4px;&#10;        }&#10;        button {&#10;            padding: 0.5rem 1.2rem;&#10;            border: none;&#10;            border-radius: 4px;&#10;            cursor: pointer;&#10;            font-weight: bold;&#10;        }&#10;        /* Primary detect button */&#10;        #detect-form button[type=&quot;submit&quot;] {&#10;            background-color: #2b2d42;&#10;            color: white;&#10;        }&#10;        #detect-form button[type=&quot;submit&quot;]:hover {&#10;            background-color: #1c1d30;&#10;        }&#10;        /* Record and stop buttons */&#10;        #start-record {&#10;            background-color: #28a745;&#10;            color: white;&#10;            margin-left: 0.5rem;&#10;        }&#10;        #start-record:hover {&#10;            background-color: #218838;&#10;        }&#10;        #stop-record {&#10;            background-color: #dc3545;&#10;            color: white;&#10;            margin-left: 0.5rem;&#10;        }&#10;        #stop-record:hover {&#10;            background-color: #c82333;&#10;        }&#10;        /* Generate button */&#10;        #generate-form button[type=&quot;submit&quot;] {&#10;            background-color: #6f42c1;&#10;            color: white;&#10;        }&#10;        #generate-form button[type=&quot;submit&quot;]:hover {&#10;            background-color: #5936a2;&#10;        }&#10;        .result {&#10;            margin-top: 1rem;&#10;            padding: 1rem;&#10;            background-color: #e9ecef;&#10;            border-radius: 4px;&#10;        }&#10;        /* Loading spinner */&#10;        #loading-spinner {&#10;            display: none;&#10;            position: fixed;&#10;            top: 0;&#10;            left: 0;&#10;            width: 100vw;&#10;            height: 100vh;&#10;            background: rgba(255, 255, 255, 0.7);&#10;            z-index: 9999;&#10;            align-items: center;&#10;            justify-content: center;&#10;            font-size: 2rem;&#10;        }&#10;    &lt;/style&gt;&#10;&lt;/head&gt;&#10;&lt;body&gt;&#10;    &lt;header&gt;&#10;        &lt;h1&gt;Voice Deepfake Vishing Detector &amp; Generator&lt;/h1&gt;&#10;    &lt;/header&gt;&#10;    &lt;main&gt;&#10;        &lt;section id=&quot;detect-section&quot;&gt;&#10;            &lt;h2&gt;1. Detect Deepfake Audio&lt;/h2&gt;&#10;            &lt;p style=&quot;font-size: 0.9rem; color: #555;&quot;&gt;Upload a WAV file or use your microphone to record a few seconds of speech, then click &lt;strong&gt;Detect&lt;/strong&gt;. &lt;/p&gt;&#10;            &lt;p style=&quot;font-size: 0.8rem; color: #a00; margin-top:-0.5rem;&quot;&gt;Note: Many browsers record in WebM format by default, which this demo server cannot decode. For real‑time demos, please ensure your browser can record as WAV or plan to transcode the recording on the server using a tool like FFmpeg or pydub.&lt;/p&gt;&#10;            &lt;p style=&quot;font-size: 0.9rem; color: #a00;&quot;&gt;Note: You must use the form below to upload a WAV file for detection. Detection by URL or query string is not supported.&lt;/p&gt;&#10;            &lt;form id=&quot;detect-form&quot; enctype=&quot;multipart/form-data&quot;&gt;&#10;                &lt;label for=&quot;detect-audio&quot;&gt;Upload WAV audio:&lt;/label&gt;&#10;                &lt;input type=&quot;file&quot; id=&quot;detect-audio&quot; name=&quot;audio&quot; accept=&quot;audio/wav&quot; /&gt;&#10;                &lt;div style=&quot;margin-top:0.5rem;&quot;&gt;&#10;                    &lt;button type=&quot;submit&quot;&gt;Detect&lt;/button&gt;&#10;                    &lt;button type=&quot;button&quot; id=&quot;start-record&quot;&gt;Record&lt;/button&gt;&#10;                    &lt;button type=&quot;button&quot; id=&quot;stop-record&quot; disabled&gt;Stop&lt;/button&gt;&#10;                &lt;/div&gt;&#10;            &lt;/form&gt;&#10;            &lt;div id=&quot;detect-result&quot; class=&quot;result&quot; style=&quot;display:none;&quot;&gt;&lt;/div&gt;&#10;        &lt;/section&gt;&#10;        &lt;hr /&gt;&#10;        &lt;section id=&quot;generate-section&quot;&gt;&#10;            &lt;h2&gt;2. Generate Deepfake Audio&lt;/h2&gt;&#10;            &lt;p style=&quot;font-size: 0.9rem; color: #555;&quot;&gt;Provide a source WAV file and some text. The server will generate a pitch‑shifted &quot;deepfake&quot; voice sample from your input.&lt;/p&gt;&#10;            &lt;form id=&quot;generate-form&quot; enctype=&quot;multipart/form-data&quot;&gt;&#10;                &lt;label for=&quot;generate-audio&quot;&gt;Upload WAV audio (source):&lt;/label&gt;&#10;                &lt;input type=&quot;file&quot; id=&quot;generate-audio&quot; name=&quot;audio&quot; accept=&quot;audio/wav&quot; required /&gt;&#10;                &lt;label for=&quot;generate-text&quot;&gt;Enter text to modulate:&lt;/label&gt;&#10;                &lt;input type=&quot;text&quot; id=&quot;generate-text&quot; name=&quot;text&quot; placeholder=&quot;Type your prompt here...&quot; required /&gt;&#10;                &lt;button type=&quot;submit&quot;&gt;Generate&lt;/button&gt;&#10;            &lt;/form&gt;&#10;            &lt;div id=&quot;generate-result&quot; class=&quot;result&quot; style=&quot;display:none;&quot;&gt;&lt;/div&gt;&#10;        &lt;/section&gt;&#10;    &lt;/main&gt;&#10;    &lt;div id=&quot;loading-spinner&quot; style=&quot;display:none;position:fixed;top:0;left:0;width:100vw;height:100vh;background:rgba(255,255,255,0.7);z-index:9999;align-items:center;justify-content:center;font-size:2rem;&quot;&gt;&lt;span&gt;Loading, please wait...&lt;/span&gt;&lt;/div&gt;&#10;    &lt;script&gt;&#10;        function showLoading() {&#10;            document.getElementById('loading-spinner').style.display = 'flex';&#10;        }&#10;        function hideLoading() {&#10;            document.getElementById('loading-spinner').style.display = 'none';&#10;        }&#10;        // Handle detection form submission&#10;        document.getElementById('detect-form').addEventListener('submit', function(event) {&#10;            event.preventDefault();&#10;            console.log('Detect form submitted');&#10;            showLoading();&#10;            const formData = new FormData();&#10;            const fileInput = document.getElementById('detect-audio');&#10;            if (fileInput.files.length &gt; 0) {&#10;                const file = fileInput.files[0];&#10;                if (file.type !== 'audio/wav' &amp;&amp; file.type !== 'audio/wave' &amp;&amp; !file.name.endsWith('.wav')) {&#10;                    hideLoading();&#10;                    alert('Please upload a WAV file. Only WAV format is supported.');&#10;                    return false;&#10;                }&#10;                formData.append('audio', file);&#10;                fetch('/detect', { method: 'POST', body: formData })&#10;                .then(response =&gt; response.json())&#10;                .then(data =&gt; {&#10;                    hideLoading();&#10;                    showDetectResult(data);&#10;                })&#10;                .catch(err =&gt; { hideLoading(); console.error(err); });&#10;            } else {&#10;                hideLoading();&#10;                alert('Please select a WAV file to upload.');&#10;            }&#10;            return false;&#10;        });&#10;&#10;        function showDetectResult(data) {&#10;            const resultDiv = document.getElementById('detect-result');&#10;            resultDiv.style.display = 'block';&#10;            if (data.message) {&#10;                resultDiv.innerHTML = `&lt;strong&gt;${data.message}&lt;/strong&gt;`;&#10;            } else if (data.prediction !== undefined) {&#10;                let message = '';&#10;                if (data.prediction === 0 || data.prediction === '0') {&#10;                    message = 'Actual (real) voice detected.';&#10;                } else if (data.prediction === 1 || data.prediction === '1') {&#10;                    message = 'Deepfake voice detected!';&#10;                } else {&#10;                    message = `Prediction: ${data.prediction}`;&#10;                }&#10;                resultDiv.innerHTML = `&lt;strong&gt;${message}&lt;/strong&gt;`;&#10;            } else if (data.error) {&#10;                resultDiv.innerHTML = `&lt;strong&gt;Error:&lt;/strong&gt; ${data.error}`;&#10;            } else {&#10;                resultDiv.innerHTML = `&lt;strong&gt;Error:&lt;/strong&gt; Unknown error`;&#10;            }&#10;        }&#10;&#10;        // Microphone recording for detection&#10;        let mediaRecorder;&#10;        let recordedChunks = [];&#10;        const startBtn = document.getElementById('start-record');&#10;        const stopBtn = document.getElementById('stop-record');&#10;        startBtn.addEventListener('click', function() {&#10;            navigator.mediaDevices.getUserMedia({ audio: true }).then(stream =&gt; {&#10;                mediaRecorder = new MediaRecorder(stream);&#10;                recordedChunks = [];&#10;                mediaRecorder.ondataavailable = function(e) {&#10;                    if (e.data.size &gt; 0) recordedChunks.push(e.data);&#10;                };&#10;                mediaRecorder.onstop = function() {&#10;                    const blob = new Blob(recordedChunks, { type: 'audio/webm' });&#10;                    const file = new File([blob], 'recording.webm', { type: 'audio/webm' });&#10;                    const formData = new FormData();&#10;                    formData.append('audio', file);&#10;                    fetch('/detect', { method: 'POST', body: formData })&#10;                    .then(res =&gt; res.json())&#10;                    .then data =&gt; {&#10;                        showDetectResult(data);&#10;                    })&#10;                    .catch(err =&gt; console.error(err));&#10;                };&#10;                mediaRecorder.start();&#10;                startBtn.disabled = true;&#10;                stopBtn.disabled = false;&#10;            }).catch(err =&gt; {&#10;                alert('Microphone access denied: ' + err);&#10;            });&#10;        });&#10;        stopBtn.addEventListener('click', function() {&#10;            if (mediaRecorder &amp;&amp; mediaRecorder.state !== 'inactive') {&#10;                mediaRecorder.stop();&#10;                startBtn.disabled = false;&#10;                stopBtn.disabled = true;&#10;            }&#10;        });&#10;&#10;        // Handle generation form submission&#10;        document.getElementById('generate-form').addEventListener('submit', function(event) {&#10;            event.preventDefault();&#10;            showLoading();&#10;            const formData = new FormData();&#10;            const fileInput = document.getElementById('generate-audio');&#10;            const textInput = document.getElementById('generate-text');&#10;            formData.append('audio', fileInput.files[0]);&#10;            formData.append('text', textInput.value);&#10;            fetch('/generate', {&#10;                method: 'POST',&#10;                body: formData&#10;            })&#10;            .then(response =&gt; response.json())&#10;            .then(data =&gt; {&#10;                hideLoading();&#10;                const resultDiv = document.getElementById('generate-result');&#10;                resultDiv.style.display = 'block';&#10;                if (data.generated_file) {&#10;                    const audio = document.createElement('audio');&#10;                    audio.controls = true;&#10;                    audio.src = data.generated_file;&#10;                    resultDiv.innerHTML = '';&#10;                    resultDiv.appendChild(document.createTextNode('Deepfake generated! '));&#10;                    resultDiv.appendChild(audio);&#10;                } else {&#10;                    resultDiv.innerHTML = `&lt;strong&gt;Error:&lt;/strong&gt; ${data.error}`;&#10;                }&#10;            })&#10;            .catch(err =&gt; { hideLoading(); console.error(err); });&#10;        });&#10;    &lt;/script&gt;&#10;&lt;/body&gt;&#10;&lt;/html&gt;" />
              <option name="updatedContent" value="&lt;!DOCTYPE html&gt;&#10;&lt;html lang=&quot;en&quot;&gt;&#10;&lt;head&gt;&#10;    &lt;meta charset=&quot;UTF-8&quot; /&gt;&#10;    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&gt;&#10;    &lt;title&gt;Deepfake Vishing Demo&lt;/title&gt;&#10;    &lt;style&gt;&#10;        body {&#10;            font-family: Arial, sans-serif;&#10;            background-color: #f5f7fa;&#10;            margin: 0;&#10;            padding: 0;&#10;        }&#10;        header {&#10;            background-color: #2b2d42;&#10;            color: white;&#10;            padding: 1rem;&#10;            text-align: center;&#10;        }&#10;        main {&#10;            max-width: 800px;&#10;            margin: 2rem auto;&#10;            background: white;&#10;            border-radius: 8px;&#10;            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);&#10;            padding: 2rem;&#10;        }&#10;        h2 {&#10;            color: #2b2d42;&#10;            margin-top: 0;&#10;        }&#10;        form {&#10;            margin-bottom: 2rem;&#10;        }&#10;        label {&#10;            display: block;&#10;            margin-bottom: 0.5rem;&#10;            font-weight: bold;&#10;        }&#10;        input[type=&quot;file&quot;],&#10;        input[type=&quot;text&quot;] {&#10;            width: 100%;&#10;            padding: 0.5rem;&#10;            margin-bottom: 1rem;&#10;            border: 1px solid #ccc;&#10;            border-radius: 4px;&#10;        }&#10;        button {&#10;            padding: 0.5rem 1.2rem;&#10;            border: none;&#10;            border-radius: 4px;&#10;            cursor: pointer;&#10;            font-weight: bold;&#10;        }&#10;        /* Primary detect button */&#10;        #detect-form button[type=&quot;submit&quot;] {&#10;            background-color: #2b2d42;&#10;            color: white;&#10;        }&#10;        #detect-form button[type=&quot;submit&quot;]:hover {&#10;            background-color: #1c1d30;&#10;        }&#10;        /* Record and stop buttons */&#10;        #start-record {&#10;            background-color: #28a745;&#10;            color: white;&#10;            margin-left: 0.5rem;&#10;        }&#10;        #start-record:hover {&#10;            background-color: #218838;&#10;        }&#10;        #stop-record {&#10;            background-color: #dc3545;&#10;            color: white;&#10;            margin-left: 0.5rem;&#10;        }&#10;        #stop-record:hover {&#10;            background-color: #c82333;&#10;        }&#10;        /* Generate button */&#10;        #generate-form button[type=&quot;submit&quot;] {&#10;            background-color: #6f42c1;&#10;            color: white;&#10;        }&#10;        #generate-form button[type=&quot;submit&quot;]:hover {&#10;            background-color: #5936a2;&#10;        }&#10;        .result {&#10;            margin-top: 1rem;&#10;            padding: 1rem;&#10;            background-color: #e9ecef;&#10;            border-radius: 4px;&#10;        }&#10;        /* Loading spinner */&#10;        #loading-spinner {&#10;            display: none;&#10;            position: fixed;&#10;            top: 0;&#10;            left: 0;&#10;            width: 100vw;&#10;            height: 100vh;&#10;            background: rgba(255, 255, 255, 0.7);&#10;            z-index: 9999;&#10;            align-items: center;&#10;            justify-content: center;&#10;            font-size: 2rem;&#10;        }&#10;    &lt;/style&gt;&#10;&lt;/head&gt;&#10;&lt;body&gt;&#10;    &lt;header&gt;&#10;        &lt;h1&gt;Voice Deepfake Vishing Detector &amp; Generator&lt;/h1&gt;&#10;    &lt;/header&gt;&#10;    &lt;main&gt;&#10;        &lt;section id=&quot;detect-section&quot;&gt;&#10;            &lt;h2&gt;1. Detect Deepfake Audio&lt;/h2&gt;&#10;            &lt;p style=&quot;font-size: 0.9rem; color: #555;&quot;&gt;Upload a WAV file or use your microphone to record a few seconds of speech, then click &lt;strong&gt;Detect&lt;/strong&gt;. &lt;/p&gt;&#10;            &lt;p style=&quot;font-size: 0.8rem; color: #a00; margin-top:-0.5rem;&quot;&gt;Note: Many browsers record in WebM format by default, which this demo server cannot decode. For real‑time demos, please ensure your browser can record as WAV or plan to transcode the recording on the server using a tool like FFmpeg or pydub.&lt;/p&gt;&#10;            &lt;p style=&quot;font-size: 0.9rem; color: #a00;&quot;&gt;Note: You must use the form below to upload a WAV file for detection. Detection by URL or query string is not supported.&lt;/p&gt;&#10;            &lt;form id=&quot;detect-form&quot; enctype=&quot;multipart/form-data&quot; onsubmit=&quot;return false;&quot;&gt;&#10;                &lt;label for=&quot;detect-audio&quot;&gt;Upload WAV audio:&lt;/label&gt;&#10;                &lt;input type=&quot;file&quot; id=&quot;detect-audio&quot; name=&quot;audio&quot; accept=&quot;audio/wav&quot; /&gt;&#10;                &lt;div style=&quot;margin-top:0.5rem;&quot;&gt;&#10;                    &lt;button type=&quot;submit&quot;&gt;Detect&lt;/button&gt;&#10;                    &lt;button type=&quot;button&quot; id=&quot;start-record&quot;&gt;Record&lt;/button&gt;&#10;                    &lt;button type=&quot;button&quot; id=&quot;stop-record&quot; disabled&gt;Stop&lt;/button&gt;&#10;                &lt;/div&gt;&#10;            &lt;/form&gt;&#10;            &lt;div id=&quot;detect-result&quot; class=&quot;result&quot; style=&quot;display:none;&quot;&gt;&lt;/div&gt;&#10;        &lt;/section&gt;&#10;        &lt;hr /&gt;&#10;        &lt;section id=&quot;generate-section&quot;&gt;&#10;            &lt;h2&gt;2. Generate Deepfake Audio&lt;/h2&gt;&#10;            &lt;p style=&quot;font-size: 0.9rem; color: #555;&quot;&gt;Provide a source WAV file and some text. The server will generate a pitch‑shifted &quot;deepfake&quot; voice sample from your input.&lt;/p&gt;&#10;            &lt;form id=&quot;generate-form&quot; enctype=&quot;multipart/form-data&quot; onsubmit=&quot;return false;&quot;&gt;&#10;                &lt;label for=&quot;generate-audio&quot;&gt;Upload WAV audio (source):&lt;/label&gt;&#10;                &lt;input type=&quot;file&quot; id=&quot;generate-audio&quot; name=&quot;audio&quot; accept=&quot;audio/wav&quot; required /&gt;&#10;                &lt;label for=&quot;generate-text&quot;&gt;Enter text to modulate:&lt;/label&gt;&#10;                &lt;input type=&quot;text&quot; id=&quot;generate-text&quot; name=&quot;text&quot; placeholder=&quot;Type your prompt here...&quot; required /&gt;&#10;                &lt;button type=&quot;submit&quot;&gt;Generate&lt;/button&gt;&#10;            &lt;/form&gt;&#10;            &lt;div id=&quot;generate-result&quot; class=&quot;result&quot; style=&quot;display:none;&quot;&gt;&lt;/div&gt;&#10;        &lt;/section&gt;&#10;    &lt;/main&gt;&#10;    &lt;div id=&quot;loading-spinner&quot; style=&quot;display:none;position:fixed;top:0;left:0;width:100vw;height:100vh;background:rgba(255,255,255,0.7);z-index:9999;align-items:center;justify-content:center;font-size:2rem;&quot;&gt;&lt;span&gt;Loading, please wait...&lt;/span&gt;&lt;/div&gt;&#10;    &lt;script&gt;&#10;        console.log('JS loaded');&#10;        function showLoading() {&#10;            document.getElementById('loading-spinner').style.display = 'flex';&#10;        }&#10;        function hideLoading() {&#10;            document.getElementById('loading-spinner').style.display = 'none';&#10;        }&#10;        // Handle detection form submission&#10;        document.getElementById('detect-form').addEventListener('submit', function(event) {&#10;            event.preventDefault();&#10;            console.log('Detect form submitted');&#10;            showLoading();&#10;            const formData = new FormData();&#10;            const fileInput = document.getElementById('detect-audio');&#10;            if (fileInput.files.length &gt; 0) {&#10;                const file = fileInput.files[0];&#10;                if (file.type !== 'audio/wav' &amp;&amp; file.type !== 'audio/wave' &amp;&amp; !file.name.endsWith('.wav')) {&#10;                    hideLoading();&#10;                    alert('Please upload a WAV file. Only WAV format is supported.');&#10;                    return false;&#10;                }&#10;                formData.append('audio', file);&#10;                fetch('/detect', { method: 'POST', body: formData })&#10;                .then(response =&gt; response.json())&#10;                .then(data =&gt; {&#10;                    hideLoading();&#10;                    showDetectResult(data);&#10;                })&#10;                .catch(err =&gt; { hideLoading(); console.error(err); });&#10;            } else {&#10;                hideLoading();&#10;                alert('Please select a WAV file to upload.');&#10;            }&#10;            return false;&#10;        });&#10;&#10;        function showDetectResult(data) {&#10;            const resultDiv = document.getElementById('detect-result');&#10;            resultDiv.style.display = 'block';&#10;            if (data.message) {&#10;                resultDiv.innerHTML = `&lt;strong&gt;${data.message}&lt;/strong&gt;`;&#10;            } else if (data.prediction !== undefined) {&#10;                let message = '';&#10;                if (data.prediction === 0 || data.prediction === '0') {&#10;                    message = 'Actual (real) voice detected.';&#10;                } else if (data.prediction === 1 || data.prediction === '1') {&#10;                    message = 'Deepfake voice detected!';&#10;                } else {&#10;                    message = `Prediction: ${data.prediction}`;&#10;                }&#10;                resultDiv.innerHTML = `&lt;strong&gt;${message}&lt;/strong&gt;`;&#10;            } else if (data.error) {&#10;                resultDiv.innerHTML = `&lt;strong&gt;Error:&lt;/strong&gt; ${data.error}`;&#10;            } else {&#10;                resultDiv.innerHTML = `&lt;strong&gt;Error:&lt;/strong&gt; Unknown error`;&#10;            }&#10;        }&#10;&#10;        // Microphone recording for detection&#10;        let mediaRecorder;&#10;        let recordedChunks = [];&#10;        const startBtn = document.getElementById('start-record');&#10;        const stopBtn = document.getElementById('stop-record');&#10;        startBtn.addEventListener('click', function() {&#10;            navigator.mediaDevices.getUserMedia({ audio: true }).then(stream =&gt; {&#10;                mediaRecorder = new MediaRecorder(stream);&#10;                recordedChunks = [];&#10;                mediaRecorder.ondataavailable = function(e) {&#10;                    if (e.data.size &gt; 0) recordedChunks.push(e.data);&#10;                };&#10;                mediaRecorder.onstop = function() {&#10;                    const blob = new Blob(recordedChunks, { type: 'audio/webm' });&#10;                    const file = new File([blob], 'recording.webm', { type: 'audio/webm' });&#10;                    const formData = new FormData();&#10;                    formData.append('audio', file);&#10;                    fetch('/detect', { method: 'POST', body: formData })&#10;                    .then(res =&gt; res.json())&#10;                    .then data =&gt; {&#10;                        showDetectResult(data);&#10;                    })&#10;                    .catch(err =&gt; console.error(err));&#10;                };&#10;                mediaRecorder.start();&#10;                startBtn.disabled = true;&#10;                stopBtn.disabled = false;&#10;            }).catch(err =&gt; {&#10;                alert('Microphone access denied: ' + err);&#10;            });&#10;        });&#10;        stopBtn.addEventListener('click', function() {&#10;            if (mediaRecorder &amp;&amp; mediaRecorder.state !== 'inactive') {&#10;                mediaRecorder.stop();&#10;                startBtn.disabled = false;&#10;                stopBtn.disabled = true;&#10;            }&#10;        });&#10;&#10;        // Handle generation form submission&#10;        document.getElementById('generate-form').addEventListener('submit', function(event) {&#10;            event.preventDefault();&#10;            showLoading();&#10;            const formData = new FormData();&#10;            const fileInput = document.getElementById('generate-audio');&#10;            const textInput = document.getElementById('generate-text');&#10;            formData.append('audio', fileInput.files[0]);&#10;            formData.append('text', textInput.value);&#10;            fetch('/generate', {&#10;                method: 'POST',&#10;                body: formData&#10;            })&#10;            .then(response =&gt; response.json())&#10;            .then(data =&gt; {&#10;                hideLoading();&#10;                const resultDiv = document.getElementById('generate-result');&#10;                resultDiv.style.display = 'block';&#10;                if (data.generated_file) {&#10;                    const audio = document.createElement('audio');&#10;                    audio.controls = true;&#10;                    audio.src = data.generated_file;&#10;                    resultDiv.innerHTML = '';&#10;                    resultDiv.appendChild(document.createTextNode('Deepfake generated! '));&#10;                    resultDiv.appendChild(audio);&#10;                } else {&#10;                    resultDiv.innerHTML = `&lt;strong&gt;Error:&lt;/strong&gt; ${data.error}`;&#10;                }&#10;            })&#10;            .catch(err =&gt; { hideLoading(); console.error(err); });&#10;        });&#10;    &lt;/script&gt;&#10;&lt;/body&gt;&#10;&lt;/html&gt;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/pipeline.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/pipeline.py" />
              <option name="originalContent" value="&quot;&quot;&quot;&#10;Pipeline module for the Voice Deepfake Vishing Detection &amp; Response System.&#10;&#10;This module exposes two primary functions:&#10;&#10;1. **detect_deepfake(audio_path)** – Given a path to a `.wav` file, the function&#10;   reads the audio, extracts key speech features (MFCCs, spectral features,&#10;   jitter, shimmer) and uses a pre‑trained classifier to determine whether&#10;   the sample is likely a real or deepfake voice.&#10;2. **generate_deepfake(input_audio_path, output_audio_path, text)** – Given an&#10;   input recording and a text prompt, this function synthesizes a &quot;deepfake&quot;&#10;   version of the recording by applying a pitch shift and noise. In a fully&#10;   featured system, you would integrate a text‑to‑speech model here. For&#10;   demonstration purposes, we modulate the pitch based on the text length and&#10;   add minor noise to create a synthetic variant of the voice.&#10;&#10;The pipeline assumes the classifier model (`deepfake_detector.pkl`) exists in&#10;the same directory. If you retrain the model, make sure to overwrite the&#10;pickle file.&#10;&quot;&quot;&quot;&#10;&#10;import numpy as np&#10;import joblib&#10;from scipy.io import wavfile&#10;from scipy import signal&#10;import scipy.fftpack as fftpack&#10;import os&#10;from gtts import gTTS&#10;from pydub import AudioSegment&#10;import traceback&#10;&#10;&quot;&quot;&quot;&#10;This module implements feature extraction, deepfake detection, and simple&#10;voice‐modification for demonstration purposes. To improve generalisation,&#10;audio is resampled to a fixed sample rate (16 kHz) prior to feature&#10;extraction. When retraining the model on new data (e.g. Open Speech&#10;Repository recordings), save the resulting pickle file alongside this&#10;module and update ``MODEL_PATH`` accordingly.&#10;&quot;&quot;&quot;&#10;&#10;# Default model path. Update this constant if you train a new model&#10;# (e.g. ``deepfake_detector_osr.pkl``) and wish to use it for inference.&#10;MODEL_PATH = 'deepfake_detector_osr.pkl'&#10;&#10;&#10;def load_model():&#10;    &quot;&quot;&quot;Load the pre‑trained deepfake detection model.&quot;&quot;&quot;&#10;    return joblib.load(MODEL_PATH)&#10;&#10;&#10;def compute_features(segment: np.ndarray, sr: int):&#10;    &quot;&quot;&quot;Compute MFCCs, spectral features, jitter, and shimmer for a given audio segment.&#10;&#10;    Parameters&#10;    ----------&#10;    segment : np.ndarray&#10;        The audio signal segment (1D array).&#10;    sr : int&#10;        Sample rate of the audio.&#10;&#10;    Returns&#10;    -------&#10;    np.ndarray&#10;        A 1D feature vector containing 13 MFCCs followed by spectral centroid,&#10;        spectral bandwidth, spectral rolloff, jitter and shimmer.&#10;    &quot;&quot;&quot;&#10;    # Pre‑emphasis filter&#10;    pre_emphasis = 0.97&#10;    emphasized = np.append(segment[0], segment[1:] - pre_emphasis * segment[:-1])&#10;&#10;    # Framing parameters&#10;    frame_length = 0.025  # 25ms&#10;    frame_step = 0.010    # 10ms&#10;    frame_length_samples = int(round(frame_length * sr))&#10;    frame_step_samples = int(round(frame_step * sr))&#10;    signal_length = len(emphasized)&#10;    num_frames = int(np.ceil(float(np.abs(signal_length - frame_length_samples)) / frame_step_samples))&#10;    pad_signal_length = num_frames * frame_step_samples + frame_length_samples&#10;    pad_signal = np.append(emphasized, np.zeros(pad_signal_length - signal_length))&#10;&#10;    # Create frames&#10;    indices = (np.tile(np.arange(0, frame_length_samples), (num_frames, 1)) +&#10;               np.tile(np.arange(0, num_frames * frame_step_samples, frame_step_samples), (frame_length_samples, 1)).T)&#10;    frames = pad_signal[indices.astype(np.int32, copy=False)]&#10;    frames *= np.hamming(frame_length_samples)&#10;&#10;    # FFT and power spectrum&#10;    NFFT = 512&#10;    mag_frames = np.absolute(np.fft.rfft(frames, NFFT))&#10;    pow_frames = ((1.0 / NFFT) * (mag_frames ** 2))&#10;&#10;    # Mel filterbank parameters&#10;    nfilt = 26&#10;    low_freq_mel = 0&#10;    high_freq_mel = (2595 * np.log10(1 + (sr / 2) / 700))&#10;    mel_points = np.linspace(low_freq_mel, high_freq_mel, nfilt + 2)&#10;    hz_points = 700 * (10 ** (mel_points / 2595) - 1)&#10;    bin_freqs = np.floor((NFFT + 1) * hz_points / sr).astype(int)&#10;    fbank = np.zeros((nfilt, int(np.floor(NFFT / 2 + 1))))&#10;    for m in range(1, nfilt + 1):&#10;        f_m_minus = bin_freqs[m - 1]&#10;        f_m = bin_freqs[m]&#10;        f_m_plus = bin_freqs[m + 1]&#10;        for k in range(f_m_minus, f_m):&#10;            fbank[m - 1, k] = (k - bin_freqs[m - 1]) / (bin_freqs[m] - bin_freqs[m - 1])&#10;        for k in range(f_m, f_m_plus):&#10;            fbank[m - 1, k] = (bin_freqs[m + 1] - k) / (bin_freqs[m + 1] - bin_freqs[m])&#10;&#10;    filter_banks = np.dot(pow_frames, fbank.T)&#10;    filter_banks = np.where(filter_banks == 0, np.finfo(float).eps, filter_banks)&#10;    filter_banks = 20 * np.log10(filter_banks)&#10;&#10;    mfcc = fftpack.dct(filter_banks, type=2, axis=1, norm='ortho')[:, :13]&#10;    mfcc_mean = np.mean(mfcc, axis=0)&#10;&#10;    # Spectral features&#10;    spectrum = np.abs(np.fft.rfft(segment))&#10;    freqs = np.fft.rfftfreq(len(segment), d=1.0 / sr)&#10;    centroid = np.sum(freqs * spectrum) / np.sum(spectrum)&#10;    bandwidth = np.sqrt(np.sum(((freqs - centroid) ** 2) * spectrum) / np.sum(spectrum))&#10;    cumulative_energy = np.cumsum(spectrum)&#10;    rolloff_threshold = 0.85 * cumulative_energy[-1]&#10;    rolloff_index = np.where(cumulative_energy &gt;= rolloff_threshold)[0][0]&#10;    rolloff = freqs[rolloff_index]&#10;&#10;    # Jitter and shimmer approximations&#10;    diff_signal = np.diff(segment)&#10;    jitter = np.mean(np.abs(diff_signal))&#10;    envelope = np.abs(segment)&#10;    shimmer = np.std(np.diff(envelope))&#10;&#10;    return np.concatenate([mfcc_mean, [centroid, bandwidth, rolloff, jitter, shimmer]])&#10;&#10;&#10;def detect_deepfake(audio_path: str) -&gt; str:&#10;    &quot;&quot;&quot;Classify an audio file as 'real' or 'fake'.&#10;&#10;    Parameters&#10;    ----------&#10;    audio_path : str&#10;        Path to the audio file (.wav) to classify.&#10;&#10;    Returns&#10;    -------&#10;    str&#10;        Prediction label ('real' or 'fake').&#10;    &quot;&quot;&quot;&#10;    sr, data = wavfile.read(audio_path)&#10;    # Convert to mono if the audio has multiple channels&#10;    if data.ndim &gt; 1:&#10;        data = data.mean(axis=1)&#10;    # Convert sample type to float and normalise&#10;    data = data.astype(np.float32)&#10;    max_val = np.max(np.abs(data))&#10;    if max_val &gt; 0:&#10;        data /= max_val&#10;    # Target sample rate for feature extraction&#10;    target_sr = 16000&#10;    if sr != target_sr:&#10;        # Resample to the target sample rate&#10;        new_length = int(len(data) * target_sr / sr)&#10;        data = signal.resample(data, new_length)&#10;        sr = target_sr&#10;    # Use a one‑second segment; zero‑pad if shorter&#10;    segment_length = sr  # one second in samples&#10;    if len(data) &gt;= segment_length:&#10;        segment = data[:segment_length]&#10;    else:&#10;        segment = np.pad(data, (0, segment_length - len(data)), mode='constant')&#10;    features = compute_features(segment, sr)&#10;    # Load the classifier and predict&#10;    model = load_model()&#10;    pred = model.predict([features])[0]&#10;    return pred&#10;&#10;&#10;# Add Coqui TTS integration&#10;try:&#10;    from TTS.api import TTS&#10;    coqui_tts_available = True&#10;except ImportError:&#10;    coqui_tts_available = False&#10;&#10;&#10;def ensure_mono_16k(input_audio_path: str) -&gt; str:&#10;    &quot;&quot;&quot;Convert input audio to mono, 16kHz WAV if needed. Returns new path if converted, else original.&quot;&quot;&quot;&#10;    audio = AudioSegment.from_file(input_audio_path)&#10;    if audio.channels != 1 or audio.frame_rate != 16000:&#10;        converted_path = input_audio_path.replace('.wav', '_mono16k.wav')&#10;        audio = audio.set_channels(1).set_frame_rate(16000)&#10;        audio.export(converted_path, format=&quot;wav&quot;)&#10;        return converted_path&#10;    return input_audio_path&#10;&#10;&#10;def generate_deepfake(input_audio_path: str, output_audio_path: str, text: str) -&gt; None:&#10;    &quot;&quot;&quot;Create a synthetic deepfake version of an input audio file using Coqui TTS if available.&quot;&quot;&quot;&#10;    print(f&quot;[generate_deepfake] Input: {input_audio_path}, Output: {output_audio_path}, Text: {text}&quot;)&#10;    used_fallback = False&#10;    # Ensure input audio is mono/16kHz&#10;    input_audio_path = ensure_mono_16k(input_audio_path)&#10;    if coqui_tts_available:&#10;        print(&quot;[generate_deepfake] Using Coqui TTS for voice cloning.&quot;)&#10;        try:&#10;            tts = TTS(model_name=&quot;tts_models/multilingual/multi-dataset/your_tts&quot;, progress_bar=False)&#10;            tts.tts_to_file(text=text, speaker_wav=input_audio_path, file_path=output_audio_path, language=&quot;en&quot;)&#10;            if not os.path.exists(output_audio_path):&#10;                raise RuntimeError(f&quot;[!] Model file not found in the output path after synthesis. Check if input audio is clean (mono, 16kHz, single speaker) and ffmpeg is installed.&quot;)&#10;        except Exception as e:&#10;            print(f&quot;[generate_deepfake] Coqui TTS failed: {e}. Falling back to gTTS.&quot;)&#10;            traceback.print_exc()&#10;            used_fallback = True&#10;    else:&#10;        used_fallback = True&#10;    if used_fallback:&#10;        print(&quot;[generate_deepfake] Using gTTS for text-to-speech.&quot;)&#10;        tts = gTTS(text=text, lang='en')&#10;        tts.save(output_audio_path)&#10;    print(f&quot;[generate_deepfake] Output file created: {output_audio_path}, size: {os.path.getsize(output_audio_path) if os.path.exists(output_audio_path) else 'N/A'} bytes&quot;)&#10;&#10;&#10;def detect_deepfake_audio(audio_path: str) -&gt; str:&#10;    &quot;&quot;&quot;Detect if the given audio file is a deepfake or real using the trained model.&quot;&quot;&quot;&#10;    sr, data = wavfile.read(audio_path)&#10;    # Ensure mono&#10;    if len(data.shape) &gt; 1:&#10;        data = data[:, 0]&#10;    # Use the whole audio, regardless of length&#10;    segment = data&#10;    features = compute_features(segment, sr)&#10;    model = load_model()&#10;    pred = model.predict([features])[0]&#10;    return 'deepfake' if pred == 1 else 'real'&#10;" />
              <option name="updatedContent" value="&quot;&quot;&quot;&#10;Pipeline module for the Voice Deepfake Vishing Detection &amp; Response System.&#10;&#10;This module exposes two primary functions:&#10;&#10;1. **detect_deepfake(audio_path)** – Given a path to a `.wav` file, the function&#10;   reads the audio, extracts key speech features (MFCCs, spectral features,&#10;   jitter, shimmer) and uses a pre‑trained classifier to determine whether&#10;   the sample is likely a real or deepfake voice.&#10;2. **generate_deepfake(input_audio_path, output_audio_path, text)** – Given an&#10;   input recording and a text prompt, this function synthesizes a &quot;deepfake&quot;&#10;   version of the recording by applying a pitch shift and noise. In a fully&#10;   featured system, you would integrate a text‑to‑speech model here. For&#10;   demonstration purposes, we modulate the pitch based on the text length and&#10;   add minor noise to create a synthetic variant of the voice.&#10;&#10;The pipeline assumes the classifier model (`deepfake_detector.pkl`) exists in&#10;the same directory. If you retrain the model, make sure to overwrite the&#10;pickle file.&#10;&quot;&quot;&quot;&#10;&#10;import numpy as np&#10;import joblib&#10;from scipy.io import wavfile&#10;from scipy import signal&#10;import scipy.fftpack as fftpack&#10;import os&#10;from gtts import gTTS&#10;from pydub import AudioSegment&#10;import traceback&#10;&#10;&quot;&quot;&quot;&#10;This module implements feature extraction, deepfake detection, and simple&#10;voice‐modification for demonstration purposes. To improve generalisation,&#10;audio is resampled to a fixed sample rate (16 kHz) prior to feature&#10;extraction. When retraining the model on new data (e.g. Open Speech&#10;Repository recordings), save the resulting pickle file alongside this&#10;module and update ``MODEL_PATH`` accordingly.&#10;&quot;&quot;&quot;&#10;&#10;# Default model path. Update this constant if you train a new model&#10;# (e.g. ``deepfake_detector_osr.pkl``) and wish to use it for inference.&#10;MODEL_PATH = 'deepfake_detector_osr.pkl'&#10;&#10;&#10;def load_model():&#10;    &quot;&quot;&quot;Load the pre‑trained deepfake detection model.&quot;&quot;&quot;&#10;    return joblib.load(MODEL_PATH)&#10;&#10;&#10;def compute_features(segment: np.ndarray, sr: int):&#10;    &quot;&quot;&quot;Compute MFCCs, spectral features, jitter, and shimmer for a given audio segment.&#10;&#10;    Parameters&#10;    ----------&#10;    segment : np.ndarray&#10;        The audio signal segment (1D array).&#10;    sr : int&#10;        Sample rate of the audio.&#10;&#10;    Returns&#10;    -------&#10;    np.ndarray&#10;        A 1D feature vector containing 13 MFCCs followed by spectral centroid,&#10;        spectral bandwidth, spectral rolloff, jitter and shimmer.&#10;    &quot;&quot;&quot;&#10;    # Pre‑emphasis filter&#10;    pre_emphasis = 0.97&#10;    emphasized = np.append(segment[0], segment[1:] - pre_emphasis * segment[:-1])&#10;&#10;    # Framing parameters&#10;    frame_length = 0.025  # 25ms&#10;    frame_step = 0.010    # 10ms&#10;    frame_length_samples = int(round(frame_length * sr))&#10;    frame_step_samples = int(round(frame_step * sr))&#10;    signal_length = len(emphasized)&#10;    num_frames = int(np.ceil(float(np.abs(signal_length - frame_length_samples)) / frame_step_samples))&#10;    pad_signal_length = num_frames * frame_step_samples + frame_length_samples&#10;    pad_signal = np.append(emphasized, np.zeros(pad_signal_length - signal_length))&#10;&#10;    # Create frames&#10;    indices = (np.tile(np.arange(0, frame_length_samples), (num_frames, 1)) +&#10;               np.tile(np.arange(0, num_frames * frame_step_samples, frame_step_samples), (frame_length_samples, 1)).T)&#10;    frames = pad_signal[indices.astype(np.int32, copy=False)]&#10;    frames *= np.hamming(frame_length_samples)&#10;&#10;    # FFT and power spectrum&#10;    NFFT = 512&#10;    mag_frames = np.absolute(np.fft.rfft(frames, NFFT))&#10;    pow_frames = ((1.0 / NFFT) * (mag_frames ** 2))&#10;&#10;    # Mel filterbank parameters&#10;    nfilt = 26&#10;    low_freq_mel = 0&#10;    high_freq_mel = (2595 * np.log10(1 + (sr / 2) / 700))&#10;    mel_points = np.linspace(low_freq_mel, high_freq_mel, nfilt + 2)&#10;    hz_points = 700 * (10 ** (mel_points / 2595) - 1)&#10;    bin_freqs = np.floor((NFFT + 1) * hz_points / sr).astype(int)&#10;    fbank = np.zeros((nfilt, int(np.floor(NFFT / 2 + 1))))&#10;    for m in range(1, nfilt + 1):&#10;        f_m_minus = bin_freqs[m - 1]&#10;        f_m = bin_freqs[m]&#10;        f_m_plus = bin_freqs[m + 1]&#10;        for k in range(f_m_minus, f_m):&#10;            fbank[m - 1, k] = (k - bin_freqs[m - 1]) / (bin_freqs[m] - bin_freqs[m - 1])&#10;        for k in range(f_m, f_m_plus):&#10;            fbank[m - 1, k] = (bin_freqs[m + 1] - k) / (bin_freqs[m + 1] - bin_freqs[m])&#10;&#10;    filter_banks = np.dot(pow_frames, fbank.T)&#10;    filter_banks = np.where(filter_banks == 0, np.finfo(float).eps, filter_banks)&#10;    filter_banks = 20 * np.log10(filter_banks)&#10;&#10;    mfcc = fftpack.dct(filter_banks, type=2, axis=1, norm='ortho')[:, :13]&#10;    mfcc_mean = np.mean(mfcc, axis=0)&#10;&#10;    # Spectral features&#10;    spectrum = np.abs(np.fft.rfft(segment))&#10;    freqs = np.fft.rfftfreq(len(segment), d=1.0 / sr)&#10;    centroid = np.sum(freqs * spectrum) / np.sum(spectrum)&#10;    bandwidth = np.sqrt(np.sum(((freqs - centroid) ** 2) * spectrum) / np.sum(spectrum))&#10;    cumulative_energy = np.cumsum(spectrum)&#10;    rolloff_threshold = 0.85 * cumulative_energy[-1]&#10;    rolloff_index = np.where(cumulative_energy &gt;= rolloff_threshold)[0][0]&#10;    rolloff = freqs[rolloff_index]&#10;&#10;    # Jitter and shimmer approximations&#10;    diff_signal = np.diff(segment)&#10;    jitter = np.mean(np.abs(diff_signal))&#10;    envelope = np.abs(segment)&#10;    shimmer = np.std(np.diff(envelope))&#10;&#10;    return np.concatenate([mfcc_mean, [centroid, bandwidth, rolloff, jitter, shimmer]])&#10;&#10;&#10;def detect_deepfake(audio_path: str) -&gt; str:&#10;    &quot;&quot;&quot;Classify an audio file as 'real' or 'fake'.&#10;&#10;    Parameters&#10;    ----------&#10;    audio_path : str&#10;        Path to the audio file (.wav) to classify.&#10;&#10;    Returns&#10;    -------&#10;    str&#10;        Prediction label ('real' or 'fake').&#10;    &quot;&quot;&quot;&#10;    sr, data = wavfile.read(audio_path)&#10;    # Convert to mono if the audio has multiple channels&#10;    if data.ndim &gt; 1:&#10;        data = data.mean(axis=1)&#10;    # Convert sample type to float and normalise&#10;    data = data.astype(np.float32)&#10;    max_val = np.max(np.abs(data))&#10;    if max_val &gt; 0:&#10;        data /= max_val&#10;    # Target sample rate for feature extraction&#10;    target_sr = 16000&#10;    if sr != target_sr:&#10;        # Resample to the target sample rate&#10;        new_length = int(len(data) * target_sr / sr)&#10;        data = signal.resample(data, new_length)&#10;        sr = target_sr&#10;    # Use a one‑second segment; zero‑pad if shorter&#10;    segment_length = sr  # one second in samples&#10;    if len(data) &gt;= segment_length:&#10;        segment = data[:segment_length]&#10;    else:&#10;        segment = np.pad(data, (0, segment_length - len(data)), mode='constant')&#10;    features = compute_features(segment, sr)&#10;    # Ensure features are a DataFrame with correct column names&#10;    import pandas as pd&#10;    feature_names = [&#10;        'MFCC1','MFCC2','MFCC3','MFCC4','MFCC5','MFCC6','MFCC7','MFCC8','MFCC9','MFCC10','MFCC11','MFCC12','MFCC13',&#10;        'centroid','bandwidth','rolloff','jitter','shimmer'&#10;    ]&#10;    features_df = pd.DataFrame([features], columns=feature_names)&#10;    # Load the classifier and predict&#10;    model = load_model()&#10;    pred = model.predict(features_df)[0]&#10;    return pred&#10;&#10;&#10;# Add Coqui TTS integration&#10;try:&#10;    from TTS.api import TTS&#10;    coqui_tts_available = True&#10;except ImportError:&#10;    coqui_tts_available = False&#10;&#10;&#10;def ensure_mono_16k(input_audio_path: str) -&gt; str:&#10;    &quot;&quot;&quot;Convert input audio to mono, 16kHz WAV if needed. Returns new path if converted, else original.&quot;&quot;&quot;&#10;    audio = AudioSegment.from_file(input_audio_path)&#10;    if audio.channels != 1 or audio.frame_rate != 16000:&#10;        converted_path = input_audio_path.replace('.wav', '_mono16k.wav')&#10;        audio = audio.set_channels(1).set_frame_rate(16000)&#10;        audio.export(converted_path, format=&quot;wav&quot;)&#10;        return converted_path&#10;    return input_audio_path&#10;&#10;&#10;def generate_deepfake(input_audio_path: str, output_audio_path: str, text: str) -&gt; None:&#10;    &quot;&quot;&quot;Create a synthetic deepfake version of an input audio file using Coqui TTS if available.&quot;&quot;&quot;&#10;    print(f&quot;[generate_deepfake] Input: {input_audio_path}, Output: {output_audio_path}, Text: {text}&quot;)&#10;    used_fallback = False&#10;    # Ensure input audio is mono/16kHz&#10;    input_audio_path = ensure_mono_16k(input_audio_path)&#10;    if coqui_tts_available:&#10;        print(&quot;[generate_deepfake] Using Coqui TTS for voice cloning.&quot;)&#10;        try:&#10;            tts = TTS(model_name=&quot;tts_models/multilingual/multi-dataset/your_tts&quot;, progress_bar=False)&#10;            tts.tts_to_file(text=text, speaker_wav=input_audio_path, file_path=output_audio_path, language=&quot;en&quot;)&#10;            if not os.path.exists(output_audio_path):&#10;                raise RuntimeError(f&quot;[!] Model file not found in the output path after synthesis. Check if input audio is clean (mono, 16kHz, single speaker) and ffmpeg is installed.&quot;)&#10;        except Exception as e:&#10;            print(f&quot;[generate_deepfake] Coqui TTS failed: {e}. Falling back to gTTS.&quot;)&#10;            traceback.print_exc()&#10;            used_fallback = True&#10;    else:&#10;        used_fallback = True&#10;    if used_fallback:&#10;        print(&quot;[generate_deepfake] Using gTTS for text-to-speech.&quot;)&#10;        tts = gTTS(text=text, lang='en')&#10;        tts.save(output_audio_path)&#10;    print(f&quot;[generate_deepfake] Output file created: {output_audio_path}, size: {os.path.getsize(output_audio_path) if os.path.exists(output_audio_path) else 'N/A'} bytes&quot;)&#10;&#10;&#10;def detect_deepfake_audio(audio_path: str) -&gt; str:&#10;    &quot;&quot;&quot;Detect if the given audio file is a deepfake or real using the trained model.&quot;&quot;&quot;&#10;    sr, data = wavfile.read(audio_path)&#10;    # Ensure mono&#10;    if len(data.shape) &gt; 1:&#10;        data = data[:, 0]&#10;    # Use the whole audio, regardless of length&#10;    segment = data&#10;    features = compute_features(segment, sr)&#10;    model = load_model()&#10;    pred = model.predict([features])[0]&#10;    return 'deepfake' if pred == 1 else 'real'" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/server.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/server.py" />
              <option name="originalContent" value="#!/usr/bin/env python3&#10;&quot;&quot;&quot;&#10;Deepfake Vishing Detection &amp; Response demo server.&#10;Uses python-multipart's parse_form_data helper and returns JSON on *all* responses.&#10;&quot;&quot;&quot;&#10;&#10;import json&#10;import os&#10;import sys&#10;import uuid&#10;import traceback&#10;from http.server import HTTPServer, SimpleHTTPRequestHandler&#10;from io import BytesIO&#10;import cgi&#10;from urllib.parse import urlparse&#10;&#10;# Ensure we can import your pipeline module&#10;CURRENT_DIR = os.path.dirname(os.path.abspath(__file__))&#10;PARENT_DIR  = os.path.dirname(CURRENT_DIR)&#10;if PARENT_DIR not in sys.path:&#10;    sys.path.append(PARENT_DIR)&#10;&#10;from pipeline import detect_deepfake, generate_deepfake&#10;&#10;UPLOAD_DIR    = os.path.join(CURRENT_DIR, 'uploads')&#10;GENERATED_DIR = os.path.join(CURRENT_DIR, 'web_app', 'generated')&#10;os.makedirs(UPLOAD_DIR, exist_ok=True)&#10;os.makedirs(GENERATED_DIR, exist_ok=True)&#10;&#10;class DeepfakeHandler(SimpleHTTPRequestHandler):&#10;    def _set_cors_headers(self):&#10;        self.send_header('Access-Control-Allow-Origin', '*')&#10;        self.send_header('Access-Control-Allow-Methods', 'GET, POST, OPTIONS')&#10;        self.send_header('Access-Control-Allow-Headers', 'Content-Type')&#10;&#10;    def _json_response(self, code:int, payload:dict):&#10;        body = json.dumps(payload).encode('utf-8')&#10;        print(f&quot;[DEBUG] Responding with: {body.decode('utf-8')}&quot;)  # Log response body&#10;        self.send_response(code)&#10;        self._set_cors_headers()&#10;        self.send_header('Content-Type', 'application/json')&#10;        self.send_header('Content-Length', str(len(body)))&#10;        self.end_headers()&#10;        self.wfile.write(body)&#10;&#10;    def do_OPTIONS(self):&#10;        self.send_response(200)&#10;        self._set_cors_headers()&#10;        self.end_headers()&#10;&#10;    def do_POST(self):&#10;        ct = self.headers.get('Content-Type', '')&#10;        if not ct.startswith('multipart/form-data'):&#10;            return self._json_response(400, {&quot;error&quot;:&quot;Unsupported content type&quot;})&#10;&#10;        # Use cgi.FieldStorage to parse multipart form data&#10;        form = cgi.FieldStorage(&#10;            fp=self.rfile,&#10;            headers=self.headers,&#10;            environ={&#10;                'REQUEST_METHOD': 'POST',&#10;                'CONTENT_TYPE': self.headers['Content-Type'],&#10;            }&#10;        )&#10;&#10;        # Extract fields and files&#10;        fields = {}&#10;        files = {}&#10;        for key in form.keys():&#10;            item = form[key]&#10;            if item.filename:&#10;                files[key] = item&#10;            else:&#10;                fields[key] = item.value&#10;&#10;        # DEBUG: print to console&#10;        print(&quot;FIELDS:&quot;, fields)&#10;        print(&quot;FILES:&quot;, list(files.keys()))&#10;&#10;        if self.path == '/detect':&#10;            if 'audio' not in files:&#10;                return self._json_response(400, {&quot;error&quot;:&quot;Missing audio file&quot;})&#10;            audio = files['audio']&#10;            name  = os.path.basename(audio.filename)&#10;            uid   = uuid.uuid4().hex&#10;            in_p  = os.path.join(UPLOAD_DIR, f&quot;{uid}_{name}&quot;)&#10;            with open(in_p,'wb') as f: f.write(audio.file.read())&#10;&#10;            try:&#10;                pred = detect_deepfake(in_p)&#10;                if hasattr(pred,'item'): pred = pred.item()&#10;                # Always return a user-friendly message&#10;                if str(pred) == '0':&#10;                    result_msg = &quot;Actual (real) voice detected.&quot;&#10;                elif str(pred) == '1':&#10;                    result_msg = &quot;Deepfake voice detected!&quot;&#10;                else:&#10;                    result_msg = f&quot;Prediction: {pred}&quot;&#10;                return self._json_response(200, {&quot;prediction&quot;: pred, &quot;message&quot;: result_msg})&#10;            except Exception as e:&#10;                traceback.print_exc()&#10;                return self._json_response(500, {&quot;error&quot;:str(e)})&#10;&#10;        elif self.path == '/generate':&#10;            if 'audio' not in files or 'text' not in fields:&#10;                return self._json_response(400, {&quot;error&quot;:&quot;Missing audio file or text&quot;})&#10;            audio = files['audio']&#10;            text  = fields['text']&#10;            name = os.path.basename(audio.filename)&#10;            uid  = uuid.uuid4().hex&#10;            in_p = os.path.join(UPLOAD_DIR, f&quot;{uid}_{name}&quot;)&#10;            with open(in_p,'wb') as f: f.write(audio.file.read())&#10;&#10;            out_name = f&quot;deepfake_{uid}_{name}&quot;&#10;            out_p    = os.path.join(GENERATED_DIR, out_name)&#10;            try:&#10;                generate_deepfake(in_p, out_p, text)&#10;                if not os.path.exists(out_p):&#10;                    raise RuntimeError(&quot;Generated file missing&quot;)&#10;                return self._json_response(200, {&quot;generated_file&quot;:f&quot;/generated/{out_name}&quot;})&#10;            except Exception as e:&#10;                traceback.print_exc()&#10;                return self._json_response(500, {&quot;error&quot;:str(e)})&#10;&#10;        else:&#10;            return self._json_response(404, {&quot;error&quot;:&quot;Unknown endpoint&quot;})&#10;&#10;    def do_GET(self):&#10;        # Serve static files and generated audio as normal&#10;        if self.path.startswith('/generated/'):&#10;            fp = os.path.join(GENERATED_DIR, os.path.basename(self.path))&#10;            if os.path.exists(fp):&#10;                self.send_response(200)&#10;                self._set_cors_headers()&#10;                self.send_header('Content-Type','audio/wav')&#10;                self.end_headers()&#10;                with open(fp,'rb') as f: self.wfile.write(f.read())&#10;            else:&#10;                self._json_response(404, {&quot;error&quot;:&quot;File not found&quot;})&#10;            return&#10;        return super().do_GET()&#10;&#10;&#10;def run_server(port:int=8000):&#10;    print(f&quot;Starting server on port {port}…&quot;)&#10;    HTTPServer(('0.0.0.0',port), DeepfakeHandler).serve_forever()&#10;&#10;if __name__=='__main__':&#10;    run_server()&#10;" />
              <option name="updatedContent" value="#!/usr/bin/env python3&#10;&quot;&quot;&quot;&#10;Deepfake Vishing Detection &amp; Response demo server.&#10;Uses python-multipart's parse_form_data helper and returns JSON on *all* responses.&#10;&quot;&quot;&quot;&#10;&#10;import json&#10;import os&#10;import sys&#10;import uuid&#10;import traceback&#10;from http.server import HTTPServer, SimpleHTTPRequestHandler&#10;from io import BytesIO&#10;import cgi&#10;from urllib.parse import urlparse&#10;&#10;# Ensure we can import your pipeline module&#10;CURRENT_DIR = os.path.dirname(os.path.abspath(__file__))&#10;PARENT_DIR  = os.path.dirname(CURRENT_DIR)&#10;if PARENT_DIR not in sys.path:&#10;    sys.path.append(PARENT_DIR)&#10;&#10;from pipeline import detect_deepfake, generate_deepfake&#10;&#10;UPLOAD_DIR    = os.path.join(CURRENT_DIR, 'uploads')&#10;GENERATED_DIR = os.path.join(CURRENT_DIR, 'web_app', 'generated')&#10;os.makedirs(UPLOAD_DIR, exist_ok=True)&#10;os.makedirs(GENERATED_DIR, exist_ok=True)&#10;&#10;class DeepfakeHandler(SimpleHTTPRequestHandler):&#10;    def _set_cors_headers(self):&#10;        self.send_header('Access-Control-Allow-Origin', '*')&#10;        self.send_header('Access-Control-Allow-Methods', 'GET, POST, OPTIONS')&#10;        self.send_header('Access-Control-Allow-Headers', 'Content-Type')&#10;&#10;    def _json_response(self, code:int, payload:dict):&#10;        body = json.dumps(payload).encode('utf-8')&#10;        print(f&quot;[DEBUG] Responding with: {body.decode('utf-8')}&quot;)  # Log response body&#10;        self.send_response(code)&#10;        self._set_cors_headers()&#10;        self.send_header('Content-Type', 'application/json')&#10;        self.send_header('Content-Length', str(len(body)))&#10;        self.end_headers()&#10;        self.wfile.write(body)&#10;&#10;    def do_OPTIONS(self):&#10;        self.send_response(200)&#10;        self._set_cors_headers()&#10;        self.end_headers()&#10;&#10;    def do_POST(self):&#10;        ct = self.headers.get('Content-Type', '')&#10;        if not ct.startswith('multipart/form-data'):&#10;            return self._json_response(400, {&quot;error&quot;:&quot;Unsupported content type&quot;})&#10;&#10;        # Use cgi.FieldStorage to parse multipart form data&#10;        form = cgi.FieldStorage(&#10;            fp=self.rfile,&#10;            headers=self.headers,&#10;            environ={&#10;                'REQUEST_METHOD': 'POST',&#10;                'CONTENT_TYPE': self.headers['Content-Type'],&#10;            }&#10;        )&#10;&#10;        # Extract fields and files&#10;        fields = {}&#10;        files = {}&#10;        for key in form.keys():&#10;            item = form[key]&#10;            if item.filename:&#10;                files[key] = item&#10;            else:&#10;                fields[key] = item.value&#10;&#10;        # DEBUG: print to console&#10;        print(&quot;FIELDS:&quot;, fields)&#10;        print(&quot;FILES:&quot;, list(files.keys()))&#10;&#10;        if self.path == '/detect':&#10;            if 'audio' not in files:&#10;                return self._json_response(400, {&quot;error&quot;:&quot;Missing audio file&quot;})&#10;            audio = files['audio']&#10;            name  = os.path.basename(audio.filename)&#10;            uid   = uuid.uuid4().hex&#10;            in_p  = os.path.join(UPLOAD_DIR, f&quot;{uid}_{name}&quot;)&#10;            with open(in_p,'wb') as f: f.write(audio.file.read())&#10;&#10;            try:&#10;                pred = detect_deepfake(in_p)&#10;                if hasattr(pred,'item'): pred = pred.item()&#10;                # Always return a user-friendly message&#10;                if str(pred) == '0':&#10;                    result_msg = &quot;Actual (real) voice detected.&quot;&#10;                elif str(pred) == '1':&#10;                    result_msg = &quot;Deepfake voice detected!&quot;&#10;                else:&#10;                    result_msg = f&quot;Prediction: {pred}&quot;&#10;                return self._json_response(200, {&quot;prediction&quot;: pred, &quot;message&quot;: result_msg})&#10;            except Exception as e:&#10;                traceback.print_exc()&#10;                return self._json_response(500, {&quot;error&quot;:str(e)})&#10;&#10;        elif self.path == '/generate':&#10;            if 'audio' not in files or 'text' not in fields:&#10;                return self._json_response(400, {&quot;error&quot;:&quot;Missing audio file or text&quot;})&#10;            audio = files['audio']&#10;            text  = fields['text']&#10;            name = os.path.basename(audio.filename)&#10;            uid  = uuid.uuid4().hex&#10;            in_p = os.path.join(UPLOAD_DIR, f&quot;{uid}_{name}&quot;)&#10;            with open(in_p,'wb') as f: f.write(audio.file.read())&#10;&#10;            out_name = f&quot;deepfake_{uid}_{name}&quot;&#10;            out_p    = os.path.join(GENERATED_DIR, out_name)&#10;            try:&#10;                generate_deepfake(in_p, out_p, text)&#10;                if not os.path.exists(out_p):&#10;                    raise RuntimeError(&quot;Generated file missing&quot;)&#10;                return self._json_response(200, {&quot;generated_file&quot;:f&quot;/generated/{out_name}&quot;})&#10;            except Exception as e:&#10;                traceback.print_exc()&#10;                return self._json_response(500, {&quot;error&quot;:str(e)})&#10;&#10;        else:&#10;            return self._json_response(404, {&quot;error&quot;:&quot;Unknown endpoint&quot;})&#10;&#10;    def do_GET(self):&#10;        # Serve generated audio files&#10;        if self.path.startswith('/generated/'):&#10;            fp = os.path.join(GENERATED_DIR, os.path.basename(self.path))&#10;            if os.path.exists(fp):&#10;                self.send_response(200)&#10;                self._set_cors_headers()&#10;                self.send_header('Content-Type','audio/wav')&#10;                self.end_headers()&#10;                with open(fp,'rb') as f: self.wfile.write(f.read())&#10;            else:&#10;                self._json_response(404, {&quot;error&quot;:&quot;File not found&quot;})&#10;            return&#10;        # Serve static files (css, js, etc.)&#10;        if self.path != '/' and os.path.exists(os.path.join(CURRENT_DIR, self.path.lstrip('/'))):&#10;            return super().do_GET()&#10;        # For all other GET requests (including / and any query string), serve index.html&#10;        self.send_response(200)&#10;        self._set_cors_headers()&#10;        self.send_header('Content-Type', 'text/html')&#10;        with open(os.path.join(CURRENT_DIR, 'index.html'), 'rb') as f:&#10;            content = f.read()&#10;        self.send_header('Content-Length', str(len(content)))&#10;        self.end_headers()&#10;        self.wfile.write(content)&#10;&#10;&#10;def run_server(port:int=8000):&#10;    print(f&quot;Starting server on port {port}…&quot;)&#10;    HTTPServer(('0.0.0.0',port), DeepfakeHandler).serve_forever()&#10;&#10;if __name__=='__main__':&#10;    run_server()" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>